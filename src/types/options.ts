/**
 * Illustrative option lists — confirm the real values with the company and
 * replace these. Kept in one file so the swap is a single edit.
 */
export const DEPARTMENT_OPTIONS = ['Production', 'Maintenance', 'Quality', 'Administration']

// The company refers to store locations as "Sites".
export const SITE_OPTIONS = ['Main Site', 'Warehouse A', 'Warehouse B']

export const UNIT_OPTIONS = ['pcs', 'kg', 'ltr', 'box', 'set', 'mtr']
