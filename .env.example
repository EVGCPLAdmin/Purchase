# Build-time fallback for the Sheets API backend (lowest-priority config —
# see Developer Config in-app for a per-browser override, and
# src/gas/Code.gs for the Apps Script deployment this URL points at).
VITE_SHEETS_API_URL=
